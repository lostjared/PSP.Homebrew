#include<stdio.h>
#include<stdlib.h>
#include "mfstream.h"
/*
void getline(mstring &str);
void getline(mfstream file, mstring &str);


void getline(mfstream file, mstring &str) {
	static char buf[1024];
	fgets(buf, 1023, file.fptr);
	str = buf;
}

void getline(mstring &str) {
	static char buf[1024];
	fgets(buf, 1023, stdin);
	str = buf;

}*/